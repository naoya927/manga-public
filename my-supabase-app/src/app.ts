console.log('This project uses Supabase + Vercel static hosting.');
console.log('Use `npm run build` to generate web/config.js and `npm run preview` to open the UI locally.');
console.log('Use `npm run migrate:legacy` to upload existing local data into Supabase.');
